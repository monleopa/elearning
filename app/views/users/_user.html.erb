<%= user.name %><br>
<%= user.email %>
<% if current_user&.admin? && !current_user?(user) %>
  | <%= button_to t(".delete"), user_path(user), method: :delete,
    data: {confirm: t(".confirm")} %>
<% end %>
