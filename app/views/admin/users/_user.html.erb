<%= user.name %><br>
<%= user.email %>
<% if current_user&.admin? && !current_user?(user) %>
  | <%= button_to t(".delete"), admin_destroy_user_path(locale, user),
  method: :delete, data: {confirm: t(".confirm")} %>
<% end %>
